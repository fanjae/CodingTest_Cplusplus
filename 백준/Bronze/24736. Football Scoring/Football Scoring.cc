#include <iostream>
using namespace std;
int main(void)
{
	int n = 2;
	while (n--)
	{
		int a, b, c, d, e;
		cin >> a >> b >> c >> d >> e;
		cout << a * 6 + b * 3 + c * 2 + d * 1 + e * 2 << " "; 
	}
}